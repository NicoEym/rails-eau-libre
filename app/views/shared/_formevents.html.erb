<%= simple_form_for [@swim_race, @swim_event], remote: true, html: { id: :form_new_event}  do |f| %>
  <h5>Epreuve:</h5>
  <div class="form-distance" >
    <%= f.input :distance, label: 'Distance (km):' %>
    <%= f.input :price, label: 'Price:'%>
    <%= f.input :comment, label: 'Equipement:'%>
  </div>
<% end %>
